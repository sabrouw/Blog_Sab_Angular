import { CardHoverBannersDirective } from './card-hover-banners.directive';

describe('CardHoverBannersDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHoverBannersDirective();
    expect(directive).toBeTruthy();
  });
});
